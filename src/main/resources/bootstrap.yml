--- 
management:
  security:
    enabled: false

security:
  basic:
    enabled: false

spring: 
  application: 
    name: roster
  cloud: 
    config: 
      uri: "${vcap.services.configServer.credentials.uri}"